import pymysql

# Connect to the database
connection = pymysql.connect(host='localhost',
                             user='root',
                             password='diplomskaRabota!',
                             db='diplomska')
